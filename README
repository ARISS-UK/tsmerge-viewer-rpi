
cd /opt/vc/src/hello_pi && sudo ./rebuild.sh

make

OpenVG:

sudo apt-get install libjpeg8-dev indent libfreetype6-dev ttf-dejavu-core
cd openvg/
make
make library
sudo make install

192MB memory split needed
